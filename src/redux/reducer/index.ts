import common from './commonSlice'
import initialLoad from './initialLoadingSlice'
import appTheme from './themeSlice'
import attendance from './attendanceSlice'
import auth from './authSlice'

export default { common, initialLoad, appTheme, attendance, auth }
